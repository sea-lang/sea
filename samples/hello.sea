raw[ // Includes :3
#include <stdio.h>
#include <stdbool.h>
]

/// Represents a given user :3
rec User(name: ^char, age: int)

/**
 * Main function that does main things
 */
fun main(argc: int, argv: ^char[]): int {
	let name: bool = true

	var users: User[] = [
		new User("Frodo" 50)
		new User("Gandalf" 50000)
	]

	raw[ puts("Hello, World!") ]
}
