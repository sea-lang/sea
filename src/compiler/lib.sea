use std/fs

rec Compiler(output_file: File)

// fun compile_string(code: String, output_file: File) {
// 	let lexer = make_lexer(code)
// 	let parser = make_parser(ref lexer)
// 	let compiler = make_compiler(ref parser, output_file)
// }
