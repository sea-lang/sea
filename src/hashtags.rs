pub enum FunTags {
    NoRet,
    Inline,
    Extern,
    Static,
}

pub enum RecTags {
    Union,
    Static,
}

pub enum TagTags {
    Static,
}

pub enum TagRecTags {
    Static,
}

pub enum MacTags {
    Static,
}
